const firstName = prompt("Insira o seu primeiro nome:")
const lastName = prompt("Insira o seu sobrenome:")

alert(`Nome completo: ${firstName} ${lastName}
Nome catálogo: ${lastName.toUpperCase()}, ${firstName}`)